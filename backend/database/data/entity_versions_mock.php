<?php

declare(strict_types=1);

use App\Models\Template;
use Database\Seeders\TemplateVersionsSeeder;

/**
 * Filas ad hoc para demos (`DatabaseSeeder`).
 *
 * Las versiones publicadas de {@see Template} generadas desde
 * {@see database/data/template_versions_mock.php} usan `entity_version_id` en ese mock y las crea
 * {@see TemplateVersionsSeeder} (no duplicar aquí salvo casos especiales).
 */

return [];
