<?php

declare(strict_types=1);

/**
 * Revisiones de documento (seed).
 *
 * Vaciado: las revisiones demo referenciaban documentos antiguos. Las nuevas
 * programaciones didácticas se crean como `published` directamente, sin pasar
 * por workflow de revisión en seed (se puede generar después desde la UI).
 */
return [];
