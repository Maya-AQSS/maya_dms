FROM node:22-alpine
RUN npm install -g npm@latest
WORKDIR /app
COPY package*.json ./
COPY . .
EXPOSE 5173
# Entrypoint runs npm install if needed, then starts Vite
COPY --chmod=755 docker-entrypoint.sh /usr/local/bin/docker-entrypoint.sh
ENTRYPOINT ["docker-entrypoint.sh"]
